@model MeuSite.Classes.Membros

@{
    ViewBag.Title = "AdmSala";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AdmSala</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayName("Nome")
        </th>
        <th>
            @Html.DisplayName("Email")
        </th>
        <th>
            @Html.DisplayName("Condição")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.listaPartiipantes) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idUsuario)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.senha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.conexao)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.idUsuario, sala = ViewBag.id }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.idusuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idusuario })*@
        </td>
    </tr>
}

</table>
<table>
    <tr>
        <th>
            @Html.DisplayName("Nome")
        </th>
        <th>
            @Html.DisplayName("Email")
        </th>
        <th>
            @Html.DisplayName("Condição")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.listaPendentes)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idUsuario)
            </td>
            @*<td>
                    @Html.DisplayFor(modelItem => item.email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.senha)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.conexao)
                </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.idUsuario, sala = ViewBag.id }) |
                @*@Html.ActionLink("Details", "Details", new { id=item.idusuario }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.idusuario })*@
            </td>
        </tr>
    }

</table>
